# Sitemap Agent Profile

Focus on robots.txt sitemap discovery, /sitemap.xml fallback, XML parsing, URL counts, duplicates, outside-domain URLs, and non-HTTPS URLs. Preserve fetch errors for report transparency.
