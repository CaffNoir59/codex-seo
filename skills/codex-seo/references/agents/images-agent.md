# Images Agent Profile

Focus on HTML image evidence: alt, dimensions, loading, file extensions, external hosts, generic names, and sampled content length. Avoid full image downloads in normal audits.
