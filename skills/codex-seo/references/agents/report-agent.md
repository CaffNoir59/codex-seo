# Report Agent Profile

Ensure JSON validates with Zod, HTML is standalone, issues are sorted deterministically, skipped modules and errors are explicit, and PDF generation uses Playwright from the HTML report.
