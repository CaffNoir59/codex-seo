# Google Search Console Adapter

Future scope: query performance, URL Inspection, sitemap submission state, and index coverage.

Requirements:

- OAuth or service account credentials outside the repository.
- Redacted errors.
- Deterministic JSON with date ranges and property identifiers.
- No scoring boost when data is unavailable.
