# Firecrawl Adapter

Future scope: full-site crawling and URL discovery.

The adapter should feed a URL inventory into the orchestrator without replacing URL safety checks for direct project fetches.
