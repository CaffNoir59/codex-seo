# getting started

Install with `npm install -D codex-seo`, run `npx codex-seo init --minimal`, then `npx codex-seo audit https://example.com --crawl`. Use `npx playwright install chromium` for PDF and performance features.
