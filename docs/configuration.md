# configuration

`codex-seo.config.json` uses current schemaVersion 1.1.0. Validate with `codex-seo validate --config codex-seo.config.json`; repair unambiguous changes with `--fix`. Store secrets in environment variables, never inline.
