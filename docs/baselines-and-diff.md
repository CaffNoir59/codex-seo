# baselines and diff

Create baselines with `audit --crawl --save-baseline production`. Compare with `audit --compare-baseline production` or `diff previous.json current.json`. Gates exit with code 2.
