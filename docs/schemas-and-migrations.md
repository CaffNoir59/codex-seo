# schemas and migrations

Schema versions are centralized for audit, baseline, diff, performance, GSC, history, trend, and configuration. Use `codex-seo migrate <file> --output <path>`; source files are not overwritten by default.
