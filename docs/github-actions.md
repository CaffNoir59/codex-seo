# github actions

Use `.github/workflows/codex-seo.yml` or `examples/github-actions/`. Configure `CODEX_SEO_URL`; for GSC use `GSC_SERVICE_ACCOUNT_JSON` as a GitHub Secret and delete temporary files in `if: always()` cleanup.
