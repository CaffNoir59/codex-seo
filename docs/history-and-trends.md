# history and trends

History stores `HistoryEntry` schema `1.0.0` in `.codex-seo/history`. Use `history trend`, `compare-releases`, `compare-environments`, `export-ci`, `migrate`, `prune`, and `validate`.
