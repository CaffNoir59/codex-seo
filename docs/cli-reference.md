# cli reference

Commands: `audit`, `diff`, `history`, `gsc`, `init`, `validate`, `doctor`, `migrate`, `version`. Prefer `npx codex-seo audit https://example.com --crawl`; `npm run audit -- https://example.com --crawl` remains supported.
