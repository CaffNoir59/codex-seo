# performance

Performance supports local Lighthouse, Playwright fallback, PageSpeed Insights, and CrUX. Install Chromium with `npx playwright install chromium`. Scores keep Lighthouse, internal, and field values separate.
