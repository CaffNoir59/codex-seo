# Contributing

Use Node.js 20 or newer. Run `npm run validate`, `npm run build`, and `npm run release:check` before proposing changes. Keep fixtures deterministic and never commit credentials, local reports, or history stores.